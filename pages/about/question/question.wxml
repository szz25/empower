<!--pages/about/question/question.wxml-->

<view class='question'>
  <view wx:for="{{data}}" wx:key="{{index}}">
    <view class='questions'>{{item.title}}</view>
    <view class='q-content'>{{item.content}}</view>
  </view>
</view>